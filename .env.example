# 백엔드 API (프론트에서 결제/구독·인증·데이터 모두 이 주소로 호출)
NEXT_PUBLIC_API_URL=

# 기타 (선택)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_STRICT_BACKEND_AUTH=false
NEXT_PUBLIC_AUTH_TIMEOUT_MS=8000

# NOWPayments 관련은 백엔드(.NET)에서만 사용. 프론트에는 설정하지 않음.
